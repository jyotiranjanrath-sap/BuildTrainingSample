"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.ActionResultsSegment = void 0;
const ErrorMessage_1 = require("../../../common/ErrorMessage");
const Context_1 = require("../Context");
const ISegment_1 = require("./ISegment");
/**
 * Represents an actionResult. A action result segment must start with #ActionResult.
 */
class ActionResultsSegment extends ISegment_1.ISegment {
    /**
     * Tries to resolve the segment string in the provided context.
     * @returns {IContext} A context around the client data object
     * If the process fails, the previously set error handler will be called. Always not undefined.
     */
    resolve() {
        return __awaiter(this, void 0, void 0, function* () {
            this.validateContext();
            let found = this.context.actionResults.find(element => element.name === this.specifier);
            if (!found) {
                throw new Error(ErrorMessage_1.ErrorMessage.format(ErrorMessage_1.ErrorMessage.NOT_GET_ACTION_RESULT_FOR_NAME, this.specifier));
            }
            let result = new Context_1.Context({});
            result.actionResults = this.context.actionResults;
            result.pages = this.context.pages;
            return result;
        });
    }
    validateContext() {
        if (this.context.binding || this.context.element) {
            throw new Error(ErrorMessage_1.ErrorMessage.format(ErrorMessage_1.ErrorMessage.INVALID_CONTEXT_FOR_SEGMENT, 'ActionResults'));
        }
    }
}
exports.ActionResultsSegment = ActionResultsSegment;
//# sourceMappingURL=ActionResultsSegment.js.map