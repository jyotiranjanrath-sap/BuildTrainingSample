"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.ClientDataSegment = void 0;
const ErrorMessage_1 = require("../../../common/ErrorMessage");
const Context_1 = require("../../targetpath/Context");
const ISegment_1 = require("./ISegment");
/**
 * Represents a client data segment. A client data segment must start with #ClientData.
 */
class ClientDataSegment extends ISegment_1.ISegment {
    /**
     * Tries to resolve the segment string in the provided context.
     * @returns {IContext} A context around the client data object
     * If the process fails, the previously set error handler will be called. Always not undefined.
     */
    resolve() {
        return __awaiter(this, void 0, void 0, function* () {
            this.validateContext();
            if (this.context && this.context.clientData) {
                return new Context_1.Context(this.context.clientData);
            }
            return new Context_1.Context({ actionResults: {} });
        });
    }
    isSpecifierRequired() {
        return false;
    }
    validateContext() {
        if ((!this.context.element && !this.context.binding) || this.context.element) {
            return;
        }
        throw new Error(ErrorMessage_1.ErrorMessage.NOT_GET_CLIENTDATA_FOR_CONTEXT);
    }
}
exports.ClientDataSegment = ClientDataSegment;
//# sourceMappingURL=ClientDataSegment.js.map