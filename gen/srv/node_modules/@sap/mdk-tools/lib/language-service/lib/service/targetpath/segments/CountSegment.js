"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.CountSegment = void 0;
const ErrorMessage_1 = require("../../../common/ErrorMessage");
const LanguageHelper_1 = require("../../../common/LanguageHelper");
const Context_1 = require("../../targetpath/Context");
const ISegment_1 = require("./ISegment");
/**
 * Represents a count segment. A count segment must start with #Count.
 */
class CountSegment extends ISegment_1.ISegment {
    /**
     * Tries to resolve the segment string given the context.
     * @returns {IContext} - The result of the evaluation.
     * If the evaulation is successful, a context bound to the result is returned.
     * If the process fails, an exception is thrown.
     */
    resolve() {
        return __awaiter(this, void 0, void 0, function* () {
            this.validateContext();
            return new Context_1.Context(1);
        });
    }
    isSpecifierRequired() {
        return false;
    }
    validateContext() {
        if (!this.context.element && !this.context.binding) {
            return;
        }
        let list = this.context.binding;
        if (list) {
            if (list.getValue && LanguageHelper_1.LanguageHelper.isListControl(list.type)) {
                return;
            }
            else if (list.isPropertySegment) {
                return;
            }
        }
        throw new Error(ErrorMessage_1.ErrorMessage.CONTEXT_NO_VALUE_INDEXED);
    }
}
exports.CountSegment = CountSegment;
//# sourceMappingURL=CountSegment.js.map