"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.FilterValueSegment = void 0;
const ErrorMessage_1 = require("../../../common/ErrorMessage");
const LanguageHelper_1 = require("../../../common/LanguageHelper");
const Context_1 = require("../Context");
const ISegment_1 = require("./ISegment");
/**
 * Represents a filter value segment. A filter value segment must start with #FilterValue.
 * This is used by ListPicker to get its filter value as FilterCriteria
 */
class FilterValueSegment extends ISegment_1.ISegment {
    /**
     * Tries to resolve the segment string in the context.
     * @returns {IContext} The filterCriteria value(any) of selected item
     * If the process fails, the previously set error handler will be called. Always not undefined.
     */
    resolve() {
        return __awaiter(this, void 0, void 0, function* () {
            this.validateContext();
            return new Context_1.Context({ isFilterValue: true });
        });
    }
    isSpecifierRequired() {
        return false;
    }
    validateContext() {
        if (this.context && this.context.element) {
            if (LanguageHelper_1.LanguageHelper.isListControl(this.context.element.type)) {
                return;
            }
        }
        throw new Error(ErrorMessage_1.ErrorMessage.format(ErrorMessage_1.ErrorMessage.NO_VALUE_IN_CONTROL, this.context.element));
    }
}
exports.FilterValueSegment = FilterValueSegment;
//# sourceMappingURL=FilterValueSegment.js.map