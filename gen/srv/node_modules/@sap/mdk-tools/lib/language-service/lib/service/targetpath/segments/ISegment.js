"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.ISegment = void 0;
const ErrorMessage_1 = require("../../../common/ErrorMessage");
/**
 * Common interface to represent a segment.
 */
class ISegment {
    constructor(specifier, context) {
        this.specifier = specifier;
        this.context = context;
        if (!this.context) {
            throw new Error(ErrorMessage_1.ErrorMessage.SEGMENT_CONTEXT_CANNOT_UNDEFINED);
        }
        this.throwIfSpecifierRequiredButMissing();
    }
    /**
     * By default, throw if there is no specifier.
     */
    isSpecifierRequired() {
        return true;
    }
    throwIfSpecifierRequiredButMissing() {
        if (this.isSpecifierRequired() && !this.specifier) {
            throw new Error(ErrorMessage_1.ErrorMessage.SEGMENT_SPECIFIER_CANNOT_UNDEFINED);
        }
    }
    validateContext() {
    }
}
exports.ISegment = ISegment;
//# sourceMappingURL=ISegment.js.map