"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.IndexSegment = void 0;
const ErrorMessage_1 = require("../../../common/ErrorMessage");
const LanguageHelper_1 = require("../../../common/LanguageHelper");
const Context_1 = require("../../targetpath/Context");
const ISegment_1 = require("./ISegment");
/**
 * Represents an index segment. An index segment must start with #Index.
 */
class IndexSegment extends ISegment_1.ISegment {
    /**
     * Tries to resolve the segment string given the context.
     * @returns {IContext} The object with the given index,
     *                                       in the Entity Set/List
     * If the process fails, the previously set error handler will be called. Always not undefined.
     */
    resolve() {
        return __awaiter(this, void 0, void 0, function* () {
            this.validateContext();
            let result = new Context_1.Context({});
            result.pages = this.context.pages;
            return result;
        });
    }
    getIndex() {
        return parseInt(this.specifier, 10);
    }
    validateContext() {
        let list = this.context.binding;
        if (list) {
            if (list.getValue && LanguageHelper_1.LanguageHelper.isListControl(list.type)) {
                return;
            }
            else if (list.isPropertySegment) {
                return;
            }
        }
        throw new Error(ErrorMessage_1.ErrorMessage.format(ErrorMessage_1.ErrorMessage.INVALID_CONTEXT_FOR_SEGMENT, 'Index'));
    }
}
exports.IndexSegment = IndexSegment;
//# sourceMappingURL=IndexSegment.js.map