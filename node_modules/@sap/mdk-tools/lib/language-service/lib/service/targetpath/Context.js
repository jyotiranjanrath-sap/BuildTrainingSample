"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.Context = void 0;
/**
 * A Context object groups a binding (any value or object composed of data) and
 * an optional UI element. It also holds on to some miscellaneous data that gets
 * used in rules via a ClientAPI object. A context is the result of every target
 * path segment evaluation. A ClientAPI object provides an interface to a Context
 * that can be used when evaluating rules.
 */
class Context {
    // This constructor implicitly sets properties on this object.  It is needed.  Do not delete.
    constructor(binding, element) {
        this.binding = binding;
        this.element = element;
    }
}
exports.Context = Context;
//# sourceMappingURL=Context.js.map