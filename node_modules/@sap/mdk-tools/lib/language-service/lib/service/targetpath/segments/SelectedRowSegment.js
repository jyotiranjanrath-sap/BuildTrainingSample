"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.SelectedRowSegment = void 0;
const ErrorMessage_1 = require("../../../common/ErrorMessage");
const LanguageHelper_1 = require("../../../common/LanguageHelper");
const Context_1 = require("../../targetpath/Context");
const ISegment_1 = require("./ISegment");
/**
 * Represents a selected row segment. A selected row segment must start with #SelectedRow.
 */
class SelectedRowSegment extends ISegment_1.ISegment {
    /**
     * Tries to resolve the segment string given the context.
     * @returns {IContext} - The result of the evaluation.
     * If the evaulation is successful, the context will contain
     * the result. If the process fails, an exception is thrown.
     */
    resolve() {
        return __awaiter(this, void 0, void 0, function* () {
            this.validateContext();
            let result = new Context_1.Context();
            result.pages = this.context.pages;
            result.element = this.context.element;
            return result;
        });
    }
    isSpecifierRequired() {
        return false;
    }
    validateContext() {
        const list = this.context.element;
        if (!LanguageHelper_1.LanguageHelper.isListControl(list.type)) {
            throw new Error(ErrorMessage_1.ErrorMessage.FIND_ROW_IN_NON_LIST_CONTROL_NOT_ALLOWED);
        }
    }
}
exports.SelectedRowSegment = SelectedRowSegment;
//# sourceMappingURL=SelectedRowSegment.js.map